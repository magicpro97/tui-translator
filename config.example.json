{
  "_comment": {
    "source_language": "BCP-47 code for the language spoken in the meeting. Examples: 'ja-JP' (Japanese), 'zh-CN' (Mandarin), 'ko' (Korean), 'en-US' (English).",
    "target_language": "BCP-47 code for the subtitle language you want to read. Examples: 'vi' (Vietnamese), 'en' (English), 'zh-TW' (Traditional Chinese).",
    "google_api_key": "Google Cloud API key with Speech-to-Text and Translation APIs enabled. Omit the field (remove the key entirely) to start without one — TTS and translation will be unavailable until a key is supplied. In the TUI settings editor this field is masked (shown as ••••••••); the real value is stored in plain text in config.json.",
    "tts_enabled": "Set to true to hear translated text spoken aloud via Text-to-Speech. Requires Text-to-Speech to be enabled on your API key. Default: false.",
    "tts_output_device": "Name of the audio output device to use for TTS playback. Omit the field (or set to null) to use the system default output device. The application must be restarted after changing this value.",
    "tts_routing": "TTS output routing mode (VMIC-A2). Choice values: 'speakers' | 'virtual_mic' | 'both'. Default: 'speakers' (plays through tts_output_device or system default). 'virtual_mic' routes TTS exclusively to virtual_mic_device — requires virtual_mic_device to be set. 'both' routes TTS to both tts_output_device and virtual_mic_device simultaneously — also requires virtual_mic_device. Old configs without this field automatically default to 'speakers'.",
    "virtual_mic_device": "Name of the virtual microphone device for TTS routing (VMIC-A2). Required when tts_routing is 'virtual_mic' or 'both'. Must exactly match a virtual audio device name as shown by --list-audio-devices (virtual devices are marked [VIRTUAL]). Examples: 'CABLE Input (VB-Audio Virtual Cable)', 'Line 1 (Virtual Audio Cable)', 'Voicemeeter Input (VB-Audio Voicemeeter VAIO)'. Omit (or set to null) when tts_routing is 'speakers'.",
    "virtual_device_patterns": "Optional VMIC-B2 registry for OEM/custom virtual cable names. Each enabled entry is a case-insensitive Rust regex matched against endpoint names before built-in VB-CABLE, VAC, Voicemeeter, and Generic/OEM patterns. kind values: 'vb_cable' | 'vac' | 'voicemeeter' | 'generic_oem'. Use this to add or override device-name classification without changing code; it does not install or license any vendor driver.",
    "capture_device": "Exact name of the Windows playback device to capture with WASAPI loopback. Omit the field (or set to null/blank in settings) to capture the Windows default playback device. Values must not include leading/trailing whitespace or control characters. Run 'tui-translator.exe --list-audio-devices' or press F2/Ctrl+D on this field in the settings editor to cycle through detected playback device names.",
    "cost_warning_usd": "Rolling cost-estimate threshold in USD. A warning appears in the status bar when estimated API spend exceeds this value. Set to 0 (or omit) to disable the warning.",
    "audio_source": "Audio capture backend. Choice values: 'wasapi' | 'file'. Press F2/Ctrl+D on this field in the settings editor to cycle choices. 'wasapi' (default) captures the Windows system loopback (what you hear). 'file' reads a WAV fixture from audio_file_path and loops it indefinitely — useful for soak testing without a live meeting.",
    "audio_file_path": "Path to the WAV file used when audio_source is 'file'. The file must be 16 kHz mono 16-bit PCM. Ignored (and may be omitted) when audio_source is 'wasapi'.",
    "stt_provider": "Speech-to-text provider. Choice values: 'google' | 'local'. Press F2/Ctrl+D on this field in the settings editor to cycle choices. 'google' (default) uses Google Cloud Speech-to-Text. 'local' uses CPU-local Whisper STT when the executable is built with the local-stt feature. Current releases require ggml-tiny.bin (~74 MB, multilingual) in %USERPROFILE%\\.tui-translator\\models\\. Download from https://huggingface.co/ggerganov/whisper.cpp and place the .bin file in that folder before enabling this option. If mt_provider remains 'google', Google Translation still requires google_api_key.",
    "mt_provider": "Machine-translation provider. Choice values: 'google' | 'local'. Press F2/Ctrl+D on this field in the settings editor to cycle choices. 'google' (default) uses Google Cloud Translation. 'local' uses CPU-local OPUS-MT when the executable is built with the local-mt feature and an exported Helsinki-NLP/opus-mt-ja-vi ONNX bundle is installed in %USERPROFILE%\\.tui-translator\\models\\mt\\opus-mt-ja-vi\\. The local-mt build also needs ONNX Runtime 1.20.x: place onnxruntime.dll next to tui-translator.exe or in that model folder, or set TUI_TRANSLATOR_ONNXRUNTIME_DLL.",
    "cpu_budget_pct": "CPU-usage ceiling (percent) above which local Whisper inference is skipped to protect co-running apps such as Zoom or Microsoft Teams. Active when stt_provider is 'local' and after Google STT falls back to local Whisper. 0.0 (default) disables throttling. On multi-core hosts sysinfo reports per-core percentages (e.g. 400.0 = 4 full cores); set this value accordingly. Example: 80.0 to suppress inference when the process exceeds 80% of one core.",
    "ram_budget_mb": "Process RAM ceiling in MiB. 0 (default) disables the warning. When RAM exceeds this budget the status strip warns, clears after 5% hysteresis, and optional session/audio recording is considered disabled under pressure.",
    "stt_fallback_policy": "Fallback policy when the STT provider encounters a permanent authentication error. Choice values: 'none' | 'local'. Press F2/Ctrl+D on this field in the settings editor to cycle choices. 'none' (default) halts the pipeline until the app is restarted with a valid key. 'local' automatically switches to CPU-local Whisper STT on the first Google AuthError — requires the local-stt build feature and ggml-tiny.bin in %USERPROFILE%\\.tui-translator\\models\\. Only meaningful when stt_provider is 'google'.",
    "vad": "Voice Activity Detection gate (issue #220 / EP-E.1). Set enabled=true to activate CPU-light VAD that suppresses silent chunks and brief transients before they reach the STT engine. All sub-fields are optional and fall back to sensible defaults. pre_roll_ms (unit: ms, default: 200, range: 0–2000): milliseconds of audio buffered during onset confirmation that is prepended to the STT window so leading consonants are not clipped; set to 0 to disable pre-roll.",
    "stt_phrase_hints": "Optional list of phrase hints forwarded to Google Speech-to-Text as speechContexts (issue #199). Supply meeting-specific proper nouns, product names, or terms in Japanese/Vietnamese that are frequently misrecognised. Default: [] (empty — omits speechContexts entirely). Example: [\"TuiTranslator\", \"ズームミーティング\", \"Nguyễn\"]. Changing this value requires restarting the application.",
    "session_store": "Optional transcript JSONL recording. Default: { enabled: false }, which creates no session files. Set enabled=true to write transcript-only session logs under %APPDATA%\\tui-translator\\sessions\\ on Windows or set directory to a custom folder. max_sessions caps retained JSONL files; default 100. Custom directories must not contain '..' path traversal components.",
    "audio_archive": "Optional raw audio archive (issue #228 / EP-F.3). Default: { store_audio: false, consent_given: false }, which never creates any audio files. BOTH store_audio=true AND consent_given=true must be set to enable recording. When enabled, a 16 kHz mono 16-bit PCM WAV file is written per session to %APPDATA%\\tui-translator\\audio-archive\\ on Windows (or a custom directory). The WAV is readable by the file audio source. WARNING: the archive captures every sound your speakers produced, including meeting audio, system sounds, and music. max_size_mb sets a soft per-file quota in MiB; 0 (default) means unlimited.",
    "pipeline": "Speech-window and sentence-aggregation tuning knobs (issue #267 / EP-I.4). All sub-fields are optional; omit the entire block to use built-in defaults. max_window_ms (unit: ms, default: 3000, range: 500–60000): maximum speech-window before an unconditional STT flush. early_flush_on_vad_end (default: true): when true, a VAD EndOfUtterance event flushes the speech window immediately for low-latency results; set false to let the window drain by max_window_ms/idle cadence instead. idle_flush_ms (unit: ms, default: 600, range: 50–30000): idle silence after the last chunk before flushing a partial window. idle_min_ms (unit: ms, default: 500, range: 50–30000): minimum accumulated speech before an idle flush is allowed. sentence_max_age_ms (unit: ms, default: 4000, range: 500–60000): maximum time a partial sentence fragment is held in the aggregator before being force-flushed to MT.",
    "slots": "Optional dual-slot mode configuration (DM-01). Omit this field (or set to null) to run in single-slot (legacy) mode using the top-level stt_provider, mt_provider, and target_language fields. When present, both slot_a and slot_b are required. Each slot specifies its own stt_provider ('google' | 'local'), mt_provider ('google' | 'local'), and target_language (BCP-47 code). The top-level source_language, google_api_key, and all audio/pipeline/TTS settings are shared by both slots. Equal target_language values across slots are accepted. Example: { \"slot_a\": { \"stt_provider\": \"google\", \"mt_provider\": \"google\", \"target_language\": \"vi\" }, \"slot_b\": { \"stt_provider\": \"google\", \"mt_provider\": \"google\", \"target_language\": \"en\" } }."
  },
  "source_language": "ja-JP",
  "target_language": "vi",
  "google_api_key": "YOUR_GOOGLE_API_KEY_HERE",
  "tts_enabled": false,
  "tts_routing": "speakers",
  "virtual_mic_device": null,
  "virtual_device_patterns": [],
  "capture_device": null,
  "cost_warning_usd": 1.00,
  "audio_source": "wasapi",
  "audio_file_path": null,
  "stt_provider": "google",
  "mt_provider": "google",
  "cpu_budget_pct": 0.0,
  "ram_budget_mb": 0,
  "stt_fallback_policy": "none",
  "session_store": {
    "enabled": false,
    "directory": null,
    "max_sessions": 100
  },
  "audio_archive": {
    "store_audio": false,
    "consent_given": false
  },
  "vad": {
    "enabled": false,
    "threshold": 0.01,
    "min_speech_ms": 100,
    "speech_pad_ms": 300,
    "min_silence_ms": 500,
    "pre_roll_ms": 200
  },
  "pipeline": {
    "max_window_ms": 3000,
    "early_flush_on_vad_end": true,
    "idle_flush_ms": 600,
    "idle_min_ms": 500,
    "sentence_max_age_ms": 4000
  },
  "stt_phrase_hints": []
}
